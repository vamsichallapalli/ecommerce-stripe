import { createTheme } from "@mui/system";

export  const theme = createTheme({
    breakpoints:{
        values:{
            xs:0,
            sm:480,
            md:769,
            lg:1025,
            xl:1201
        }
    }
})